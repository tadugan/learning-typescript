// A union type is OR for types
// value can satisfy either OR both conditions

// Intersection types
// value MUST satisfy both conditions
// Condition A AND Condition B

// Union types are describe using the pipe | operator
