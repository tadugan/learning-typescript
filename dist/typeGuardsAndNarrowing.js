// Type guards and narrowing
// User-defined type guards
// We could write a clunky reusable function to check our user defined types, but even with that code, TYpescript would have no way of knowing that the return 
// value of the function has anything to do with the type of the input
// User defined types in TypeScript allow us 
